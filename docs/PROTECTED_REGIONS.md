# 保護領域（プライバシー伏字）

**ステータス:** 計画（実装未着手）／差別化機能・**オープンな提案型の取り決め**
**目的:** メール本文中の機密部分を**伏字**で送り、実値は**どのメールクライアントでも開ける暗号コンテナ（暗証PDF）**で届ける。相手側の**不用意なクラウドAI処理に、プライバシーデータを自動で渡さない**ための仕組み。**将来の標準化のきっかけ**を狙う提案型（§10）。

関連: [AI_FEATURES.md](AI_FEATURES.md)（AIプライバシー方針）/ [FEATURE_SPEC.md](FEATURE_SPEC.md) / [THREADING.md](THREADING.md)

---

## 1. 目的と脅威モデル（割り切り）

- **守りたいもの**: 受信側のメールソフト/AIが本文・添付を**自動的に**クラウドAIへ送って処理する際の、機密の漏えい。
- **狙うのは「事故防止」**: 完璧な防御ではなく、**うっかり自動処理でAIに渡る**のを防ぐ。
- 本文に書いたパスワードを手動でAIに食わせ、添付まで開かせる相手は**対象外**（そこまでする相手に防御コストを払わない）。
- **確実な防御は暗号化**。伏字・記法はUXと構造であって防御ではない（暗号コンテナが防御）。

---

## 2. 記法（本文中の伏字マーカー）

```
/////privacy_address/////
```

- 区切り `/////` ＋ **任意のフィールド名** ＋ `/////`。
- プレーン/HTML どちらでも壊れにくい中立記法（タグ風 `<...>` は HTML で問題になり得るため不採用）。
- どのクライアントでも**ただの伏字テキスト**として表示される。

---

## 3. コンテナ方針（最優先＝どのクライアントでも開ける）

| コンテナ | 位置づけ |
|---|---|
| **暗証番号付き PDF（AES-256）** | **基準。** 全OS/全クライアントの標準ビューアがパスワード対応 ＝ 誰でも開ける唯一の暗号コンテナ |
| 軽量暗号パート（AES-256-GCM, `aes-gcm`） | **任意・将来の最適化。** Comfort Mail 同士のみ。最小サイズ・インライン復元向け。基準ではない |
| 暗号ZIP | 不採用（OS標準がAES非対応/弱いZipCrypto） |

> 「どのメールクライアントでも重要」を最優先するため、**基準は暗証PDF**。軽量パートは Comfort Mail 同士のおまけ（無くても成立）。

---

## 4. 送信フロー

1. 作成画面で機密スパンを選択 → 各に**任意のフィールド名**（`privacy_address` 等）を付与。
2. 本文はその位置を **`/////privacy_address/////`** で伏字化して送信。
3. 実値（フィールド名→値）を **暗証番号付きPDF（AES-256）** に格納して添付。
4. **PDFのパスワードは本文に明記**（目的は“うっかりAI処理の防止”なので許容。より固くしたい人向けに「別経路でパスワード」をオプション）。
5. 任意で**目印ヘッダ**（秘密は載せない）: `X-ComfortMail-Protected: regions=privacy_address; container=pdf`。

---

## 5. 受信フロー

- **どのクライアントでも**: 伏字本文＋本文中のパスワードで**添付PDFを開く** → 実値を読める（普遍的に成立、段階的劣化なし）。
- **Comfort Mail**: マーカーを検出し、PDF（or 軽量パート）を復号して**伏字位置に実値をインライン表示**（“随時表示”）。
- **相手のAIから見えるもの**: 伏字本文＋開けない暗号PDF ＝ **本物は渡らない** ✅

---

## 6. 核となる動作ルール（全機能で徹底）

> **保護領域は「人には実値・AIには伏字」。**

- Comfort Mail 自身のAI機能（要約・分類・下書き・[AI_FEATURES.md](AI_FEATURES.md)）に渡す前に、**保護領域を伏字へ置換**してから送る。
- 受信表示は人には実値（復号後）、しかし**AI処理時は伏字**。
- 送信本文は伏字、実値は暗号コンテナ。
- これにより「うっかりクラウドAIに本物が流れる」事故を**自動で**防止。

---

## 7. 鍵・パスワード方針

- **PDF**: パスワードは本文表示（既定）。オプションで別経路（SMS等）。
- **軽量パート（Comfort Mail 同士）**: 相手の公開鍵で暗号化＝**自動鍵交換でパスワードレス**（将来。最も滑らか）。

---

## 8. 守れること／守れないこと（正直に）

- ✅ 受信側AIの**自動処理**から機密を隠せる（暗号PDFは読めない）。
- ✅ どのクライアントでも実値にアクセスできる（普遍性）。
- ⚠️ 本文にパスワードがある以上、**狙って**復号する高度エージェントAIは理論上開ける（脅威モデル外）。
- ⚠️ 「機密が存在する事実」と前後文脈は残る（通常許容）。
- 🔒 より固くするなら：パスワード別経路、または S/MIME・PGP（標準E2E、重い）を将来オプションに。

---

## 9. 実装メモ

- **暗号化PDF生成**: Rust の多くの PDF クレートは暗号化未対応 → **qpdf 等の利用が必要**（「どこでも開ける」の対価）。AES-256 を指定（旧 RC4 は不可）。
- **軽量パート**: 既に計画済みの `aes-gcm` で容易（Comfort Mail 同士の最適化用）。
- 受信時の検出: 本文マーカー（`/////name/////`）＋目印ヘッダで判定。スキーマ変更は最小（必要なら `emails.has_protected_regions` フラグ程度）。

### Tauri コマンド（抜粋）
| コマンド | 用途 |
|---|---|
| `compose_add_protected_region` | 作成時に機密スパンを保護領域化（伏字＋実値登録） |
| `compose_build_protected` | 送信用に伏字本文＋暗証PDF（or 軽量パート）を生成 |
| `message_reveal_protected` | 受信メールの保護領域を復号してインライン表示 |
| `ai_redact_protected` | AI へ渡す前に保護領域を伏字へ置換（全AI機能が経由） |

---

## 10. オープンな提案として（標準化の種）

本機能は**特定アプリ専用の囲い込みではなく、提案型のオープンな取り決め**として設計する。普及するほど実効性が増す性質のため、誰でも実装できるよう仕様を公開する。

- **最小で誰でも実装できる**こと（記法 `/////name/////` ＋ 暗証PDF ＋ 目印ヘッダ）を重視。複雑な合意なしに片側だけでも成立する（段階的劣化）。
- **既存の近い仕組みに乗る**: 慣習ヘッダ `Sensitivity:`（Personal/Private/Confidential）や、Webで進む「AI利用の意思表示（AI preferences 等）」と思想を合わせ、孤立した独自仕様にしない。
- **コア原則をシンプルな一文に**:「**保護領域は人には実値・AIには伏字**」。この一行を他クライアントが尊重するだけで広がる。
- **普及の道筋**: ①Comfort Mail がリファレンス実装として動く → ②記法・ヘッダ・コンテナ仕様を文書公開 → ③賛同クライアントが「AIには伏字」を実装 → ④デファクト化 → ⑤必要なら標準化提案（IETF 等）。
- 目的は「**不用意なAI露出を、業界全体で減らす**」こと。Comfort Mail はその**きっかけ（提案者）**を担う。

> ねらい: 勝つための独自仕様ではなく、**広めるための提案**。シンプルさと段階的劣化が普及の鍵。
